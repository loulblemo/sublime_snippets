<snippet>
    <content><![CDATA[
import glob
import os

files = [f for f in glob.glob(os.path.join(${1:folder}, '*')) if os.path.isfile(f)]

for f in files:
    ${2:do something..}
]]></content>   
    <tabTrigger>files for</tabTrigger>
    <scope>source.python</scope>
</snippet>
